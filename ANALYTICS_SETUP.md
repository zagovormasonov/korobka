# 📊 Настройка аналитики (Analytics Events)

Эта инструкция описывает, как активировать систему отслеживания событий для воронки конверсии в CMS.

## Что реализовано

✅ **Frontend tracking** - события отправляются автоматически:
- `page_visit` - посещение главной страницы
- `test_start` - начало теста
- `test_question` - ответ на каждый вопрос (с номером вопроса и процентом прогресса)
- `test_complete` - завершение теста
- `payment_init` - открытие страницы оплаты
- `payment_success` - успешная оплата

✅ **Backend API** - endpoint `/api/analytics/track` для приёма событий

✅ **CMS дашборд** - воронка с фильтрами по времени (день, неделя, месяц, всё время)

❌ **База данных** - таблица `analytics_events` **НЕ создана**

## Как активировать

### Шаг 1: Создать таблицу в Supabase

1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте и выполните SQL из файла `server/scripts/create_analytics_events.sql`
3. Проверьте, что таблица создана: `Table Editor → analytics_events`

### Шаг 2: Проверить работу

1. Зайдите на главную страницу сайта (новая сессия)
2. Пройдите тест, ответив на несколько вопросов
3. Зайдите в CMS → Воронка
4. Если видите `"source": "analytics_events"` в данных - всё работает!
5. Если видите `"source": "fallback"` - используется старая логика (без tracking)

## Детали работы

### Fallback режим

Если таблица `analytics_events` не создана или пустая, CMS автоматически переключается на **fallback** режим:
- Воронка строится на основе данных из `primary_test_results` и `payments`
- Это менее точно, но система работает без ошибок

### Отслеживание прогресса по вопросам

Каждый вопрос теста отслеживается с метаданными:
```json
{
  "test_type": "primary",
  "question_number": 5,
  "total_questions": 20,
  "progress_percent": 25
}
```

**Это позволит в будущем:**
- Увидеть на каком вопросе люди чаще всего бросают тест
- Построить детальную воронку по каждому вопросу
- Оптимизировать порядок вопросов

### Как использовать метаданные

Для анализа прогресса по вопросам можно сделать запрос:

```sql
-- На каком вопросе чаще всего останавливаются?
SELECT 
  (metadata->>'question_number')::int as question_number,
  COUNT(DISTINCT session_id) as stopped_here
FROM analytics_events
WHERE event_type = 'test_question'
  AND session_id NOT IN (
    SELECT DISTINCT session_id 
    FROM analytics_events 
    WHERE event_type = 'test_complete'
  )
GROUP BY question_number
ORDER BY stopped_here DESC;
```

## FAQ

**Q: Что если я не создам таблицу?**
A: Всё будет работать в fallback режиме (на основе старой логики).

**Q: Можно ли мигрировать старые данные?**
A: Нет, события отслеживаются только с момента создания таблицы.

**Q: Влияет ли tracking на производительность?**
A: Нет, все запросы асинхронные и не блокируют пользователя. Даже если API упадёт, пользователь не увидит ошибок.

**Q: Когда создавать таблицу?**
A: Прямо сейчас! Чем раньше, тем больше данных соберётся для анализа.

## Следующие шаги

После активации tracking рекомендуем реализовать:

1. **Детальная воронка по вопросам** - график "где люди бросают тест"
2. **Heatmap времени** - в какое время суток больше всего активности
3. **A/B тесты** - сравнение конверсии разных вариантов
4. **Retention анализ** - сколько людей возвращается

Все эти фичи уже готовы к реализации благодаря tracking!

