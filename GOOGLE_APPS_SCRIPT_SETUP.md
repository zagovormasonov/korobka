# Инструкция по установке Google Apps Script для уведомлений в Telegram

## Шаг 1: Откройте Google Sheet, связанный с формой

1. Откройте Google Sheet, куда приходят ответы из формы
2. Если форма не связана с таблицей:
   - Откройте форму
   - Нажмите на иконку "Ответы" (зелёная таблица)
   - Нажмите на иконку "Создать таблицу" (зелёная таблица с плюсом)
   - Выберите существующую таблицу или создайте новую

## Шаг 2: Откройте Apps Script

1. В Google Sheet нажмите **Расширения** → **Apps Script**
2. Откроется редактор скриптов

## Шаг 3: Вставьте код

1. Удалите весь код по умолчанию (функцию `myFunction`)
2. Скопируйте весь код из файла `google-apps-script-telegram-form-notifications.gs`
3. Вставьте в редактор Apps Script
4. Нажмите **Сохранить** (дискета) или `Ctrl+S` / `Cmd+S`

## Шаг 4: Установите триггер

1. В редакторе Apps Script выберите функцию `setupTrigger` из выпадающего списка функций
2. Нажмите кнопку **Выполнить** (▶️)
3. При первом запуске Google попросит разрешения:
   - Нажмите **Проверить разрешения**
   - Выберите свой аккаунт Google
   - Нажмите **Дополнительно** → **Перейти к [название проекта] (небезопасно)**
   - Нажмите **Разрешить**
4. После успешного выполнения вы увидите в логах: `✅ Триггер успешно установлен!`

## Шаг 5: Проверьте работу

1. Откройте форму и отправьте тестовый ответ
2. Проверьте Telegram — должно прийти уведомление
3. Если не пришло, проверьте логи:
   - В Apps Script нажмите **Выполнение** (часы) в левом меню
   - Посмотрите последние выполнения и логи

## Дополнительные функции

### Тестовая отправка

Для проверки подключения к Telegram:
1. Выберите функцию `testTelegramConnection` из списка
2. Нажмите **Выполнить**
3. В Telegram должно прийти тестовое сообщение

### Удаление триггера

Если нужно отключить уведомления:
1. Выберите функцию `removeAllTriggers` из списка
2. Нажмите **Выполнить**
3. Все триггеры будут удалены

## Устранение проблем

### Ошибка "Telegram API вернул код 401"
- Проверьте правильность `BOT_TOKEN` в коде
- Убедитесь, что токен не содержит лишних пробелов

### Ошибка "Telegram API вернул код 400"
- Проверьте правильность `CHAT_ID` в коде
- Убедитесь, что бот добавлен в чат/группу

### Уведомления не приходят
1. Проверьте логи выполнения в Apps Script
2. Убедитесь, что триггер установлен:
   - В редакторе нажмите на иконку часов (триггеры)
   - Должен быть триггер `onFormSubmit` типа "При отправке формы"
3. Проверьте, что форма связана с этой таблицей

### Как узнать CHAT_ID
1. Напишите боту в Telegram: `@userinfobot`
2. Бот вернёт ваш `chat_id`
3. Или добавьте бота в группу и используйте ID группы

## Структура сообщения

Сообщения в Telegram будут выглядеть так:

```
🔔 Новый ответ в форме!

Вопрос 1: Как вас зовут?
Ответ: Иван

Вопрос 2: Ваш email?
Ответ: ivan@example.com

⏰ Время: 15.01.2025, 14:30:25
```

## Безопасность

⚠️ **Важно:** Не публикуйте код с токенами в публичных репозиториях!

- Токен бота даёт полный доступ к боту
- Любой, кто получит токен, сможет отправлять сообщения от имени бота
- Храните токены в секрете

## Обновление кода

Если нужно обновить код:
1. Откройте Apps Script
2. Замените старый код новым
3. Сохраните изменения
4. Триггер останется активным, переустанавливать не нужно

